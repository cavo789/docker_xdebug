{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug - Docker app",
            "type": "php",
            "request": "launch",
            "port": 9002,
            "log": true,
            "externalConsole": true,
            "ignore": ["**/vendor/**/*.php"],
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}/html"
            }
        }
    ]
}
