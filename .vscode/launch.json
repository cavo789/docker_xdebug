{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug - Docker app",
            "type": "php",
            "request": "launch",
            "port": 9001,
            "log": false,
            "externalConsole": false,
            "ignore": ["**/vendor/**/*.php"],
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}/src"
            }
        }
    ]
}
